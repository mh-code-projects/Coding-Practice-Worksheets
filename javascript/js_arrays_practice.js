// JavaScript Arrays Practice Document
// You'll use the arrays below to complete the assignments that follow

// Starter Arrays:
const fruits = ["apple", "banana", "cherry", "date", "fig"];
const numbers = [3, 7, 1, 7, 2, 9, 9, 1, 12, 5];
const users = [
  { name: "Alice", age: 25 },
  { name: "Bob", age: 32 },
  { name: "Charlie", age: 28 },
];

// ==============================
// Level 1: Basics
// ==============================
// 1. Print the first and last fruit 

// 2. Add "grape" to the end of the fruits array

// 3. Remove the first item from the numbers array  

// 4. Print the length of the fruits array  

// 5. Find the index of "cherry" 

// ==============================
// Level 2: Looping and Transforming
// ==============================
// 6. Loop through fruits and print each in uppercase 

// 7. Create a new array that doubles each number in `numbers` 

// 8. Create an array of user names (just the names)  

// 9. Filter the `users` array for users over age 30 

// 10. Sort the numbers in ascending order

// ==============================
// Level 3: Combining & Nested Logic
// ==============================
// 11. Combine the `fruits` and `numbers` arrays into one array 

// 12. Count how many fruits contain the letter "a" 

// 12b. users 

// 13. Add a new user object to the `users` array 

// 14. Create a new array of user initials (e.g. "A", "B") 

// 15. Write a function that takes an array and returns only the even numbers

//  Stretch Goals (Bonus XP)

// - Find the average age of users

// - Remove duplicates from an array like [1, 2, 2, 3, 4, 4, 5]#

// - Create a function that takes an array of strings and returns one combined sentence

// Have fun! Test, break, console.log everything.
// Arrays: like tupperware for your variables 
