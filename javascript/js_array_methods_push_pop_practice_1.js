// ===================
// Section 1: Reference
// ===================

// The push() method adds one or more elements to the end of an array. It returns the new length of the array.
// Syntax: array.push(element1, element2, ...)

// The pop() method removes the last element from an array and returns that element.
// Syntax: array.pop()

// The shift() method removes the first element from an array and returns that element.
// Syntax: array.shift()

// The unshift() method adds one or more elements to the beginning of an array. It returns the new length of the array.
// Syntax: array.unshift(element1, element2, ...)

// ===================
// Section 2: Sample Data
// ===================

let colors = ['red', 'green', 'blue', 'yellow'];
let numbers = [10, 20, 30, 40, 50];
let dailyTasks = ['wake up', 'get dressed', 'eat breakfast'];
let shoppingList = ['milk', 'bread', 'eggs'];
let queue = []; // an empty array to be used as a queue

// =========================
// Section 3: Challenge Prompts
// =========================

// **Individual Method Challenges (1-10)**

// Challenge 1: Add a new color, 'purple', to the end of the colors array.

// Challenge 2: Remove the last element from the numbers array.

// Challenge 3: Remove the first element from the dailyTasks array.

// Challenge 4: Add 'butter' to the beginning of the shoppingList array.

// Challenge 5: Add 'orange' and 'grape' to the end of the colors array in one go.

// Challenge 6: Remove the last item from shoppingList and store the removed item in a variable called removedItem.

// Challenge 7: Remove the first item from numbers and store the result in a variable called firstNumber.

// Challenge 8: Add the number 5 to the beginning of the numbers array.

// Challenge 9: Add 'pay bills' and 'go to gym' to the beginning of the dailyTasks array.

// Challenge 10: Remove the last two elements from the colors array.

// ---

// **Combined Method Challenges (11-20)**

// Challenge 11: Add 'apple' to the end of the queue array, then remove the first item and store it in a new variable called firstInQueue.

// Challenge 12: Add the number 100 to the end of the numbers array, then remove the first number and add it back to the end of the array.

// Challenge 13: Take the last item from shoppingList and add it to the front of the array.

// Challenge 14: Add 'learn to code' to the beginning of the dailyTasks array, then remove the last two items.

// Challenge 15: Create a new array called tempItems. Take the first three items from the colors array and add them to tempItems one by one.

// Challenge 16: Simulate a stack (Last-In, First-Out). Push 'a', 'b', and 'c' onto an empty array, then use pop() twice to remove the last two items.

// Challenge 17: Simulate a queue (First-In, First-Out). Add 'A', 'B', and 'C' to the queue array, then use a loop to remove and process each item one by one until the queue is empty.

// Challenge 18: Remove the last item from shoppingList and then remove the new last item as well, then add both "eggs" and "milk" back to the end of the array.

// Challenge 19: Use a combination of methods to take the first two numbers from numbers and add them to the end of the array.

// Challenge 20: Add 'meditate' to the beginning of dailyTasks, then remove the last two items, and finally add the number 7 to the end.